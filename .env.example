# Copy to .env for local runs (never commit .env). GitHub Actions uses the OPENROUTER_API_KEY secret instead.
OPENROUTER_API_KEY=
# Optional overrides (comma-separated OpenRouter model ids, tried in order)
# KHAZENDAR_MODELS_EDITOR=minimax/minimax-m3:free,nvidia/nemotron-3-ultra-550b-a55b:free
# KHAZENDAR_MODELS_WRITER=
# KHAZENDAR_MODELS_CRITIC=
# KHAZENDAR_MODELS_VISION=
